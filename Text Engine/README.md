# Text Engine
Text Engine is a C++ project that performs basic text analysis. It reads input text, removes punctuation and unnecessary characters, normalizes the words, and calculates word frequencies.

The program also reports statistics such as the total number of words before filtering, the number of valid words after filtering, and the frequency of each word in the text.
